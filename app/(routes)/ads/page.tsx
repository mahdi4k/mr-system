import React, { } from 'react';
import type { Metadata } from 'next'



export const metadata: Metadata = {
    title: 'آگهی قطعات',
    description: '',
}
const Page: React.FC = () => {

    return (
        <PageClient />
    );
};

export default Page;
